<%= form_for(@suggestion_state) do |f| %>
  <% if @suggestion_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@suggestion_state.errors.count, "error") %> prohibited this suggestion_state from being saved:</h2>

      <ul>
      <% @suggestion_state.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :done %><br>
    <%= f.check_box :done %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
